// Add a comment somewhere within the provided div element, without modifying the existing h1 or p elements.

const JSX = (
  <div>
  
  {/* This is how comments are written in JSX */}
  
    <h1>This is a block of JSX</h1>
    <p>Here's a subtitle</p>
  </div>
);
